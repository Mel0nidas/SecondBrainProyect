[project]
name = "segundo-cerebro"
version = "0.1.0"
description = "Agentes LangGraph sobre boveda Obsidian, con Telegram como interfaz de entrada"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.5.9",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "langchain-anthropic>=1.7.1",
    "langgraph>=1.2.11",
    "langgraph-checkpoint-sqlite>=3.1.1",
    "mcp[cli]>=2.1.1",
    "python-dotenv>=1.2.3",
    "uvicorn[standard]>=0.52.4",
    "voyageai>=0.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app", "src/grafo", "src/mcp_obsidian", "src/rag", "src/telegram", "src/transcripcion"]

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "tests"]
mypy_path = "src"
explicit_package_bases = true

[[tool.mypy.overrides]]
module = ["chromadb.*", "voyageai.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
